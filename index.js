const areObjects = require('are-objects');
const areArrays = require('are-arrays');

const sum = (a, b) => a + b
exports.sum = sum

const sub = (a, b) => a - b
exports.sub = sub

const mul = (a, b) => a * b
exports.mul = mul

const div = (a, b) => a / b
exports.div = div
